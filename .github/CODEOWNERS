# Review is requested automatically from the owners listed here.
#
# One line, because there is one maintainer. When that changes, replace the
# handle with a team — @fapost-lab/maintainers — so membership is managed in one
# place rather than edited into every repository.

* @bpartner
